Given /a valid user is logged in/ do
  pending
end

When /^that user creates a survey$/ do
  pending
#  @survey = Survey.create!(:question => 'Eat more chicken', :user => @user)
end

Then /^the survey should have a shortened url$/ do
  pending
#  @survey.shortened_url.should_not be_nil
end
